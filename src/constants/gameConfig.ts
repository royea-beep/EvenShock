/**
 * How long the countdown/reveal animation plays before the bot's choice and
 * round outcome are resolved. The Game screen's animation timing is driven
 * off this same constant so the reveal always lines up with when the data
 * actually becomes available.
 */
export const REVEAL_DELAY_MS = 1600;
